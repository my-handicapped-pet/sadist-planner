---
description: Start `planner-agent` for a planning task
---

Use `planner-agent` to handle this planning task.

Follow `@.junie/agents/planner-agent.md`.

Use `@.junie/templates/repo-handoff.md` when a repo handoff is
needed.

If the task involves Git or GitHub work, run the pre-flight checks in
`@.junie/workflows/local-coordination.md` before those operations.

Ask for the planning task or source file if it was not provided yet.